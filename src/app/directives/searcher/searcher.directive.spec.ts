import { SearcherDirective } from './searcher.directive';

describe('SearcherDirective', () => {
  /* it('should create an instance', () => {
    const directive = new SearcherDirective();
    expect(directive).toBeTruthy();
  }); */
});
